apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: shopflow
  labels: { app: order-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: order-service }
  template:
    metadata:
      labels: { app: order-service }
    spec:
      containers:
        - name: order-service
          # Built directly into minikube's Docker daemon (see the guide's
          # "eval $(minikube docker-env)" step) - no registry involved, so
          # imagePullPolicy must be Never or Kubernetes will try (and fail)
          # to pull "order-service:local" from Docker Hub.
          image: eks-microservices-project-order-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef: { name: shopflow-config }
            - secretRef: { name: shopflow-secrets }
          env:
            - name: DB_NAME
              value: "orderdb"
          readinessProbe:
            httpGet: { path: /health/, port: 8003 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health/, port: 8003 }
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits: { cpu: "300m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shopflow
spec:
  selector: { app: order-service }
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
